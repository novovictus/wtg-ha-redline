# WTG GPU Redline basic dashboard examples
#
# Home Assistant Manual card editor accepts one card object at a time.
# Do not paste this file directly into a Manual card.
#
# Use these single-card examples instead:
#
# - dashboards/redline-gauge-card.yaml
# - dashboards/redline-entities-card.yaml
#
# Replace YOUR_HOSTNAME in each card with the advertised hostname used when
# installing the Redline package for that host.
